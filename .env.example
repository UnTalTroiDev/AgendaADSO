# URL base de la API (solo para build/ejecución).
# En desarrollo sin valor usa el backend local por defecto.
# En Vercel/producción define la URL de tu API (ej. JSON Server en Railway, Render, etc.).
# VITE_API_URL=https://tu-api.ejemplo.com
